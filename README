This purpose of this project is to search postal code of a certain city. Hashtable was used, thus the city names are key and postal codes are the value,hashtable is a  efficient data structure that uses O(1) insert and deletion .The project was implemented in dynamic memory,each key-value pair is allocation in the heap memory.The pcode file recieves data. The data is inserted into our hashtable with the use of ht_insert().The hashtable resize everytime its 2/3 full. The ht.c has all functionalities of the hashtable like insert,delete,loop,and free- prevent a leaking memory.In pcode the user may search a key and will recieve a value which the postal codes